name: Noir tests

on:
  push:
   branches:
    - main
  pull_request:

jobs:
  test:
    name: Test on Nargo 
    uses: noir-lang/noir/.github/workflows/test.yml@master
    with:
      minimum-noir-version: v1.0.0-beta.0
